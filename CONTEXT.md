# sub2sub

通过对方授权共享的 AI 执行环境完成任务，用户留在当前工作对话中。

## Language

**使用端 / Client**：发起任务、使用共享能力并接收成果的设备。同一设备可以同时承担使用端和共享端角色。
_Avoid_：对外文案中的调用方、使用方、Caller、Consumer。

**共享端 / Host**：开放 AI 能力、接收并执行授权任务的设备；账号凭据由其所有者保管。
_Avoid_：对外文案中的提供方、执行方、Provider；插件的宿主应用不称为共享端。

**委托任务**：交给一个共享端完成的整体工作，可包含多轮指令与同一会话中的追问。

**轮次**：委托任务中的一次执行或追问；一轮结束不代表整体工作已结束。

**阶段成果**：委托任务进行到某个阶段已形成的文件和说明，可能包含尚未完成或尚未验证的工作。阶段成果已保存不等于整个任务已完成。

**跨工具委托**：使用端和共享端使用不同的 AI 工作环境，使用端将独立任务交给共享端，并接收成果及追加修改。

**工作副本**：为一个委托任务选出的必要输入及其执行中的修改，独立于使用端的源工作区。

**交付完整性检查**：核对本轮实际执行状态、结果成功取回、约定产物齐全且可读取；交付完整不等于任务必需的验证已通过。

**本地补充验证**：使用端从已取回文件建立独立验证副本，完成共享端因环境缺口无法执行的必要检查，保留插件保存的同步与恢复副本，沿用使用端的任务授权与本地权限；任务明确要求在共享端验证时不能由此替代。

**配对**：共享端授权特定使用端连接并使用共享资源的关系，可由共享端撤销。

**共享**：共享端允许已配对使用端发起委托的状态。

**执行工具**：共享端当前用于接单的 Codex 或 Claude Code；独立于管理插件的宿主，旧任务保留原工具与会话。

**节点**：独立运行的共享进程，接收委托并持有实际执行环境。停止共享只停止接单，退出节点才停止进程。

**额度**：执行工具账号提供的当前使用窗口与限制；与节点在线状态分开，多个节点可能使用同一账号。

**任务用量**：一次委托及其轮次中，执行工具报告的模型与 Token 观测；保留原生来源、分类和缺失范围，不等于账号剩余额度或价格。

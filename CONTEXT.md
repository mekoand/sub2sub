# sub2sub

通过对方授权共享的 AI 执行环境完成任务，用户留在当前工作对话中。

## Language

**调用方**：发起委托并接收产出的人及其设备。

**提供方**：授权使用自己的订阅执行委托任务的人及其设备；订阅登录由其持有。

**委托任务**：交给一个提供方完成的整体工作，可包含多轮指令与同一会话中的追问。

**轮次**：委托任务中的一次执行或追问；一轮结束不代表整体工作已结束。

**阶段成果**：委托任务进行到某个阶段已形成的文件和说明，可能包含尚未完成或尚未验证的工作。阶段成果已保存不等于整个任务已完成。

**跨工具委托**：调用方和提供方使用不同的 AI 工作环境，调用方将独立任务交给提供方，并接收成果及追加修改。

**工作副本**：为一个委托任务选出的必要输入及其执行中的修改，独立于调用方的源工作区。

**交付完整性检查**：核对主任务完成、结果成功取回、约定产物齐全且可读取；成果质量由提供方完成任务时验证。

**配对**：提供方授权特定调用方连接并使用共享资源的关系，可由提供方撤销。

**共享**：提供方允许已配对调用方发起委托的状态。

**执行工具**：提供方当前用于接单的 Codex 或 Claude Code；独立于管理插件的宿主，旧任务保留原工具与会话。

**节点**：独立运行的共享进程，接收委托并持有实际执行环境。停止共享只停止接单，退出节点才停止进程。

**额度**：执行工具账号提供的当前使用窗口与限制；与节点在线状态分开，多个节点可能使用同一账号。

**任务用量**：一次委托及其轮次中，执行工具报告的模型与 Token 观测；保留原生来源、分类和缺失范围，不等于账号剩余额度或价格。

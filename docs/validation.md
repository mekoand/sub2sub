# 兼容性与验证范围

[返回 README](../README.zh-CN.md) · [故障排查](troubleshooting.md)

以下为截至2026-09-07的验证范围。插件回归、真实任务交付和成果自身的质量检查分别记录；检查方式由任务类型决定。

## 平台

| 环境 | 已验证 | 尚不能承诺 |
| --- | --- | --- |
| macOS | 源码回归、原生插件安装、Codex CLI 实际任务、Mac到Mac的LAN配对/执行/回传/续做/清理 | Intel Mac 实机安装、所有Codex版本与所有网络拓扑 |
| Windows | Node24原生平台回归、0.5 原生安装与重复安装、此前Windows提供方及Mac到Windows生命周期 | 完整自动套件在Windows全部通过、Windows CLI 作为发起端的端到端任务 |
| Linux | Node 22、24 完整自动回归 | Linux双机及桌面插件端到端验收尚未完成 |

项目声明Node22+。实机Codex版本主要为0.152.x，使用实验性权限接口；不支持接口时会明确失败。模型以账号实际可用范围为准。

## 0.6.0 首次确认、对话升级与图标

- macOS 完整源码回归 105 项：104 项通过、1 项 Windows 专用检查跳过；语法检查通过。新增公开 Client/MCP 回归覆盖首次确认、跳过、重新引导、旧用户识别、设置保留、升级检查、真实注册来源识别、活动或不确定工作暂缓以及安装失败。安装测试发现首次状态查询写入配置并影响并发控制，已改为只读查询并通过完整回归。
- 使用发行包 Node 24.20.0、真实 Codex CLI 0.153.4，在独立临时配置和程序目录内，从公开 0.5.3 包升级到 0.6.0 候选包。原生插件列表确认新版本已启用，原配置逐字节保留，已保存文件与旧版本程序保留；安装后的三个图标路径均存在。该检查没有更改日常用户配置，也未启动模型任务。
- 真实 Claude Code 2.1.104 在独立临时配置目录安装同一候选包，确认 Claude 插件启用、Codex 注册和 sub2sub 配置保留。这只验证 Claude 调用方安装；该旧 CLI 版本不用于证明 Claude 提供方执行兼容性。
- 使用实际 Codex 注册和 GitHub 最新发行接口，公开 `update_plugin` 检查正确区分当前会话、已安装插件、停止状态的节点与最新发行版本；未触发安装。对话升级仅安装，不自动重启节点；0.5.3 需结束旧提供方会话的过渡限制已写入中英文安装说明。
- 图标通过官方清单字段校验、打包资源检查，以及明暗背景下 16、24、32、64、128 像素预览。计算机工具禁止访问 Codex 应用，因此尚未观察实际 Codex 界面的图标；#15 保留此项人工验收。
- 独立 Standards 审查无阻断问题；Spec 审查发现摘要漏列已保存网络地址和端口，已补齐并通过先失败后成功的公开工具回归。此次未连接到 Windows 实机（SSH 认证不可用），未重跑真实双机任务、跨网络 Tailscale 或真实模型执行。以下历史记录不代表本轮重复验证。

## 0.6.0 已合并的独立节点、Claude 执行与 Codex 额度

本节记录 #7、#11、#12、#13 经 #14 合并时的验证；与上方本轮新增检查分开。

- macOS 完整源码回归共 92 项：91 项通过，1 项 Windows 专用检查跳过；语法检查通过。Windows Node 24.12.0 原生平台检查 7 项全部通过，包括独立节点的启动、管理进程退出、重新连接与显式退出。
- 首轮 CI 在 macOS Node 22 上发现新任务目录建立与状态文件首次写入之间的查询竞态。任务列表现明确报告暂时不可用的记录，并保留损坏记录的错误；确定性回归和原有 Claude 停止用例通过。
- 自动回归覆盖管理进程在活动任务中退出、退出后接受新任务、并发开启只产生一个节点、忙碌时拒绝退出、更新前后运行版本区分与配对身份保留。Claude 覆盖独立默认设置、切换失败、模型查询与切换重叠、背景工作停止、取消、时限、阶段回传、原会话恢复和分层清理。时限测试通过可控时钟触发生产截止逻辑；未让真实模型空跑 30 分钟。
- macOS 使用发行包 Node 24.20.0，由真实 Claude Code 2.1.263 CLI 通过 MCP 开启节点。CLI 正常退出后，新的调用进程成功提交 Codex 文件任务、取回并核对内容，最终清理该测试的工作文件、记录和原生历史。该检查验证 CLI 宿主退出，未关闭用户正在使用的 GUI 应用。
- Windows 使用发行包 Node 24.20.0，由桌面登录会话中的真实 Codex CLI 通过 MCP 开启节点。CLI 退出后，Windows 上新的调用进程经配对 TLS 完成小文件任务、回传和定向清理，节点退出后本地文件仍可读。本次临时 Node 路径被系统防火墙单独阻止入站，因此这项发行包检查使用本机连接，未修改防火墙；下述双机结果使用 Node 24.12.0。
- macOS Claude 执行使用官方 Agent SDK 0.3.263、Claude Code 2.1.263 和真实订阅，验证工作副本内读写，范围外读写及任务网络访问被拒绝。小任务验证了活动时拒绝切换、取消后取回阶段文件、释放副本、恢复原会话并修改成果、切回 Codex 后拒绝误用旧任务。随后按任务范围删除 Claude 原生历史，本地成果保留。默认仅开放官方 Bash 工具，所有任务文件操作共用它的沙箱。
- Mac 到 Windows 的真实 Codex 任务完成输入处理、成果回传、工作副本释放、原会话恢复及第二轮修改，结果文件内容与会话 ID 均核对，测试历史清理成功。Windows 从桌面登录会话开启节点；同机 SSH 会话能读取额度，但原生 Codex 初始化在该会话中出现超时，直接调用也复现，因此不把 SSH 会话当作桌面执行环境的替代。
- macOS 和 Windows 均通过真实 Codex 本机及配对额度读取。主额度返回一周窗口（10080 分钟），Spark 类别返回 5 小时和一周窗口（300 / 10080 分钟）；只展示来源实际提供的类别、窗口和空值，不固定假设所有账号都有相同额度。读取未发起模型任务。Claude 返回明确的不支持状态。
- 独立 Standards 审查发现的旧历史识别和模型查询竞态，以及 Spec 审查发现的记录删除后清理、背景工作停止顺序、原生历史删除重试和时限错误覆盖问题，均已修正并通过对应回归。父需求复核还修正了 Claude 首次选择，未设置默认值时列出实际选项并在传输前等待选择；配对结果显示当前工具。最终定向复查无剩余阻断问题。

Claude 执行仅验证 macOS；Windows Claude 执行尚未支持。Windows 全套 POSIX 回归、跨网络 Tailscale 和 GUI 应用强制退出不属于本轮实测结论。常规清理只覆盖任务工作副本；原生历史删除为明确选择，不清理共享日志或用户备份。

## 0.5.3 Claude Code 安装与成果展示

- macOS 本地完整回归 79 项通过，语法检查通过。新增安装回归覆盖纯 Claude 调用方、双宿主入口共存、重复安装、重新启用、更新失败与重试；独立标准和规格审查的问题已修正。
- Claude Code 2.1.104 使用原生插件命令，在包含空格和中文的独立目录中完成首次安装、重复安装、随后安装 Codex、再更新 Claude。均确认插件已启用，Codex 安装未覆盖 Claude 入口；Claude 安装过程未依赖可用的本机 Codex 路径。包内 Node 为 24.20.0。
- 实际加载原生安装生成的 Claude 插件后，MCP 自动启动，完成 `answer.txt` 的生成与同任务修改；两轮成果均回传，原生会话保持一致。修正交付提示后，续作答复给出成果和答复的完整绝对本地链接。提供方关闭且测试工作文件、记录、原生历史清理后，Claude 仅调用本地 `list_tasks`，读取主成果、答复及变更清单，正确展示内容与完整链接。使用 GPT-5.6 Luna / low、本机 TLS；本轮不覆盖跨设备网络、长等待、取消 UI 或其他 Claude 产品。
- Windows 的 Claude 原生安装尚未实机验证；本轮远程连接未通过 SSH 认证。已有 Windows Codex 安装记录见下文，不等同于 Claude 安装验证。

## 0.5.2 复杂任务收尾

- macOS 本地完整回归 78 项通过，语法检查与发行目录检查通过。独立标准审查和规格审查均通过。[跨平台 CI](https://github.com/mekoand/sub2sub/actions/runs/34046196075) 的五个检查通过：macOS / Linux 各运行 Node 22、24 完整套件，Windows 运行 6 项平台测试。
- 新增回归通过可控时钟触发生产代码的 30 分钟截止路径，覆盖首次执行、同任务续作、阶段成果下载失败后的重试，以及停止执行后才保存。没有让真实模型空跑 30 分钟，也没有引入生产环境的测试时限选项。
- 内部 21 MiB 缓存不参与回传、不阻止显式或到期工作副本清理；清理后仍可恢复同任务。旧任务与其他跳过目录保留原规则。
- macOS 真实 Codex 任务使用发行包同版 Node 24.20.0 生成 21 MiB 缓存，成果回传无跳过项，工作副本清理通过。之后恢复同一原生会话、取回第二轮成果，并删除测试工作文件、记录与原生历史。连接为本机独立进程间的 TLS，本轮没有重新执行双机测试。
- Claude Code 2.1.104 通过临时 MCP 和 Skill 接入完成两轮简单文件任务：生成 `answer.txt`，再修改为新内容；两轮均主动取回并读取本地文件和答复，同一原生会话得到核对，最终测试工作文件、记录和原生历史已清理。模型为 GPT-5.6 Luna / low，连接为本机 TLS。0.5.2 安装器仅面向 Codex；该轮不覆盖 Claude 长等待、取消 UI 或其他 Claude 产品。
- Claude 的最终答复曾缩写本地文件路径。文件保存和读取通过，该问题在 0.5.3 中继续改进；该次验证不等于所有交互均已完善。

## 0.5 系列安装与 CLI

- macOS 完整源码回归 73 项通过，语法检查通过。最终 0.5.1 的[五个 CI 检查](https://github.com/mekoand/sub2sub/actions/runs/34042004850)全部通过：macOS / Linux 各运行 Node 22、24 完整套件，Windows 单独运行 6 项平台测试。[自动打包流程](https://github.com/mekoand/sub2sub/actions/runs/34042014862)成功生成三个平台的发行包。
- macOS Apple Silicon 和 Windows x64 实际运行公开 GitHub 安装脚本，完成下载、校验和原生插件安装，确认 `sub2sub@sub2sub` 0.5.1 已安装并启用。两端同版本重复安装通过，Windows 另验证了从 0.5.0 升级到 0.5.1。测试使用独立程序目录和 Codex 配置目录。
- macOS Codex CLI 使用实际安装的插件，向独立提供方进程派发文件处理任务，再从另一次 CLI 调用继续同一任务。回传文件内容核对通过，最终删除了测试的远端工作文件、记录和原生会话。本次使用本机 TLS 连接；此前双机验证记录见下文。
- macOS 真实任务中运行随包 Node，完成读取输入、生成 JSON 和回传，核对运行时为 `v24.20.0`。任务只额外获得该 Node 可执行文件的读取权限。
- 新增测试覆盖安装、重复安装、启动路径刷新、旧来源迁移、安装失败后重试，以及不依赖 PATH 中 OpenSSL 的证书生成和身份复用。Tailscale 地址测试覆盖 `100.64.0.0/10` 及相邻无效范围。
- Tailscale 实际跨网络配对、任务执行与回传留到后续版本验证。此阶段尚未实测其他 harness；后续 Claude Code 的验证见上方 0.5.2 记录。

公开安装测试发现 macOS 临时目录的路径别名会让安装入口被跳过；0.5.1 已修复，并通过实际符号链接启动脚本的回归测试覆盖。CI 中固定等待后过早关闭 MCP 客户端的测试，也已改为等待真实响应。

## 0.4.2 Windows修复

- 首次证书生成显式使用临时最小OpenSSL配置，修复外部安装的默认配置路径不存在时创建邀请失败。
- 失败保留实际可执行文件与配置来源信息，清理本次证书临时目录；已有身份无需再次生成。
- 正确发现Windows原生可执行文件，拒绝把 `.cmd` / `.bat` 当成直接执行的Codex二进制。
- 处理Windows路径分隔符和已退出进程留下的空目录锁。
- Windows包直接Node启动，写入目标设备的实际Node路径。

已有实测覆盖：默认配置缺失时邀请/TLS配对、身份复用、失败后重试、真实原生会话两轮续做、工作副本清理后恢复、最终记录/会话清理。Windows独立平台测试4项通过；历史macOS完整源码回归65项通过。发布后的CI结果以仓库Actions实际记录为准，不把历史次数当成跨平台保证。

## 0.4.3 发布前复核

- 独立复核并合成复现：记录/全部清理漏检回传后的文件变化，以及进度消息混入最终答复。两项均以最小改动修复，并增加回归测试。
- 首轮 CI 暴露了共享启动/停止时序问题；已修复本机及跨插件的过期控制请求，并用确定性测试覆盖。
- 新版本对删除范围与旧任务兼容方式保持不变；提供方明确丢弃未取回成果仍是独立选择。
- 本轮 macOS 完整源码回归 70 项通过，语法检查、插件清单校验、双平台打包及文档相对链接检查通过。
- 本轮检查使用隔离目录与模拟 Codex，没有重新执行真实订阅任务。上述 Windows 实机记录对应此前 0.4.2 的验证。

## 复杂任务体验

在Windows上用同一原生会话完成一个无外部依赖的离线排程应用，再追加工作日历与撤销重做。提供方报告36项**应用自身**测试通过并完成独立复核；这36项不是sub2sub插件回归测试。

同时观察到：

- 第一阶段触发30分钟固定上限；原任务恢复成功，未丢失工作文件。
- 第二阶段继续同一任务及会话，新增功能后完成增量回传。
- 浏览器临时profile导致结果超过20MiB，任务沙箱拒绝删除；最后在单次宿主审批后清理，正式LAN回传成功。因此不能称为全程无干预。
- 标准目录中存在Chrome/Edge，但受限执行中出现GPU初始化失败；没有有效页面截图和动态DOM结果，浏览器交互未验收。

最终已保存完整源码、测试和说明，无跳过的必要输出。上述记录只说明这次流程的实际表现，不证明任意复杂任务都能自动完成。

## 2026-09-09 双方保护核对（#27）

复用已有传输授权、路径排除、工作副本和执行权限实现，补充首次授权对提供方可见性、保留与清理范围的说明。普通文本不新增密钥扫描，也不新增权限设置或通用规则系统。

- 既有 MCP/LAN/管理页定向测试 7 项通过：传输范围与授权撤回、活动任务撤销、续跑、权限扩大拒绝、符号链接结果拒绝及离线保存成果访问。测试使用临时状态和模拟执行工具，没有模型调用。
- 以与执行适配相同的权限配置，使用原生 Codex sandbox、独立临时 HOME 和合成文件验证：工作目录内读写成功，目录外读写被拒绝且内容未变，连接测试进程已监听的本机端口被拒绝。此项没有启动模型，不能替代完整 app-server 任务验证。
- Claude 本轮核对了已有沙箱配置与模拟测试；真实 Bash 限制的历史验证见上文，本轮未重跑。
- 文件和网络沙箱不等同于环境变量隔离。合成变量在直接运行的 sandbox shell 中可见；实际 app-server 与 Claude Bash 是否进一步过滤环境，本轮没有验证，不宣称所有宿主环境秘密均不可读取。
- 开发机 Homebrew Node 23.11.0 在该 Codex 权限配置中因外部动态库读取受限而不能启动。仅允许 Node 可执行文件不保证其外部动态库可用；此前独立分发运行时的成功不能证明所有本地 Node 安装方式均受支持。本轮没有放宽整个安装目录的权限。

## 2026-09-09 节点并发（#28）

- LAN 与独立共享回归 58 项通过；新增设置竞争问题已先复现再修复，相关 3 项测试通过。覆盖默认 4 路、同一调用方占满、满额拒绝、同任务互斥、4→2 热修改、取消隔离、撤销某调用方全部活动任务、停止接单及活动任务阻止退出。
- macOS 原生 Codex 0.153.4：两项合成任务在独立工作目录中同时写出 A/B 标记；取消 A 后 B 继续执行，最终 A interrupted、B completed，取回 B 文件内容正确。使用独立分发 Node 运行时，没有放宽执行权限。
- macOS 原生 Claude 2.1.263 / SDK 0.3.263：经 MCP、独立节点及 LAN，同样确认真实 Bash 重叠、取消 A 不影响 B、A/B 成果归属和阶段文件正确。既有全局 CLI 2.1.104 不满足版本要求，本次使用官方临时原生包及现有订阅，没有修改全局安装或登录。
- Claude 实测限制：Bash 的 `/tmp/claude-501/cwd-*` 写入被现有沙箱拒绝，命令产生文件后报告退出码 1；B 在任务内重试后完成。这证明并发和成果隔离，不代表该环境的 shell 执行没有异常。
- 管理页以合成配置验证并发上限保存，在 1200 与 390 像素宽度检查设置和授权说明；未连接实际业务节点。

## 2026-09-09 Codex 任务用量（#31）

- 用量/统计回归 9 项、MCP/LAN/管理 API 集成 1 项通过。覆盖重复和交错事件、续跑、失败、取消、执行时限、缺字段、断连、实际模型切换、同步隔离和清理保留。查询与重复取回不会增加原生执行请求。
- macOS Codex 0.153.4，实际生产 Client 提交→取回→续跑→取回→统计，两轮分别为 10,747 / 11,083 Token，汇总 21,830。输入、缓存读取、缓存写入、输出、推理、原生总计六字段逐项与本次合成任务的原生日志一致。生产采集不读取日志、不额外查询账户或用量。
- 当前每轮启动独立 App Server；原生实测确认新进程恢复会话时累计计数重新开始。因此本连接内取最新累计快照，不跨轮减上个进程的累计值，不重复相加推理或缓存分类。
- 单轮进度、会话、用量和结束状态统一按顺序写入。独立审查发现的写入竞争及未同步提示已修复。管理页以合成记录验证模型分组、未知值、未取回状态和逐轮详情；导出保留相同明细和同步范围。
- 实测限于上述版本/macOS 的正常及续跑轮次；真实失败/取消、Windows 原生采集和任意工具版本未宣称通过。异常路径由协议测试覆盖；主线程计量不保证覆盖所有子代理或辅助模型调用，不计算价格。

## 自行验证

macOS/POSIX环境：

```sh
npm run check
npm test
```

Windows聚焦平台行为：

```powershell
node --test test/platform.test.mjs
```

真实订阅生命周期冒烟测试需明确授权后执行：

```sh
node scripts/lifecycle-smoke.mjs /absolute/path/to/codex
```

该命令实际使用提供方登录的订阅，执行两轮合成任务、清理工作文件并验证同会话恢复，最后删除该测试的原生历史。它不是普通单元测试，不在CI中自动执行。
